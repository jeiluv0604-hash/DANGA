# Backend API origin. Leave empty for local dev (Vite proxy forwards /api to :8000).
# For production set it to the deployed backend, e.g.:
# VITE_API_BASE_URL=https://damga-ops-api.onrender.com
VITE_API_BASE_URL=
